<Project>

  <PropertyGroup>
    <!-- ImageSharp 4 validates direct dependencies at build time. Supply the
         complete sixlabors.lic contents through this environment variable. -->
    <SixLaborsLicenseKey>$(SIXLABORS_LICENSE_KEY)</SixLaborsLicenseKey>
  </PropertyGroup>

</Project>
