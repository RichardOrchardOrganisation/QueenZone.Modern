# GitHub organization transfer cutover inventory

Prepared for [#1735](https://github.com/RichardOrchardOrganisation/QueenZone.Modern/issues/1735) and executed for [#1737](https://github.com/RichardOrchardOrganisation/QueenZone.Modern/issues/1737), part of [#1733](https://github.com/RichardOrchardOrganisation/QueenZone.Modern/issues/1733). Non-secret inventory and cutover record, 24 September 2026. The repository remains public.

GitHub [permits merge queues for public organization repositories](https://docs.github.com/en/pull-requests/how-tos/merge-and-close-pull-requests/merging-a-pull-request-with-a-merge-queue). The `RichardOrchardOrganisation` API reports the **free** plan, no existing `QueenZone.Modern` repository, and `richardorchard` as an active organization admin. Its settings page permits Actions and reusable workflows for all repositories, permits repository-level self-hosted runners for all repositories, and leaves standard hosted runners enabled. Organization member privileges permit creating public repositories. The transferred repository is public and has `main` as its default branch.

## Cutover checks

| Owner | Item and expected state | Verification | Preparation result / cutover result |
| --- | --- | --- | --- |
| Richard | Quiet window: no active CI, merge, dev or production deploy, store release, or OpenTofu apply; Windows `GLORY11` and the Mac runner accessible | Check Actions runs, PR queue, release/tag activity, runner services and both machine owners immediately before transfer | **Cutover:** all active and waiting runs cleared before transfer at about 11:35 UTC; the unapproved earlier OpenTofu apply was cancelled before any apply step. Both runners were idle. |
| Richard | Same public repository under `RichardOrchardOrganisation/QueenZone.Modern`, with no name/fork conflict | Recheck organization plan, repository list, visibility, owner/admin role, and `main` SHA | Free plan, public creation, organization admin, and absent target path verified before transfer. **Cutover:** same public repository ID `1265145026` and `main` SHA `ce3e301303d8` under the organization; old path redirects. Do not create a second repository at the old path. |
| Richard | Existing issue/PR history, tags, and releases retained | Record counts, open PR numbers, latest tag and `main` SHA just before transfer; compare at new owner | Before transfer, #1743 merged and #1746 remained open. **Cutover:** #1746 and issue history remained accessible, and latest tag `v2026.09.24.2` remained. No GitHub Releases were listed. |
| Richard | Exact `main` protection, PR settings and required checks retained | Read branch protection and repository merge settings at both owners | Strict up-to-date checks, admin enforcement, no force pushes/deletions, no PR-review requirement, no repository ruleset. Auto-merge and squash are enabled; merge commits and rebase are also allowed. **Cutover:** strict checks, admin enforcement, no force pushes/deletions, and repository merge settings retained. Queue still disabled pending #1738. |
| Richard | Twelve required checks still sourced from GitHub Actions (App ID `15368`) | Compare `main` required checks with CI job names at new owner | `build`; `test (0)`; `test (1)`; `sql-server-tests`; `coverage`; `smoke-test`; `e2e-test`; `Verify formatting`; `Small test projects (Tools/Storage/NewsAgent)`; `Mobile typecheck and unit tests`; `Mobile Android build`; `Mobile iOS build`. See #1734. **Cutover:** same twelve required check contexts retained. |
| Richard | All nine environments, branch/tag policies, reviewers, variable names and secret names retained | Compare the table below using Settings or `gh api repos/OWNER/QueenZone.Modern/environments` and each environment's variables/secrets and deployment-branch-policies endpoints | All nine inspected before transfer. **Cutover:** all nine names, branch/tag patterns, reviewers, and environment variable/secret names matched the baseline. |
| Richard | Repository Actions settings, secret and variable names retained | Compare Settings > Actions and API name lists; never export values to this document | Actions enabled, all actions allowed, default `GITHUB_TOKEN` read-only, cannot approve PR reviews. Name lists below. **Cutover:** Actions remain enabled/all allowed; default token remains read-only and cannot approve PR reviews; 14 secret and 13 variable names matched. |
| Richard | Two repository runners available with the same labels | Settings > Actions > Runners; run representative `e2e` and iOS jobs | `GLORY11` online (`Windows`, `X64`, `e2e`); `richards-mac-queenzone` online (`macOS`, `ARM64`, `queenzone`, `e2e`, `ios-signing`, `ios-build`) at snapshot. If either disappears or is disabled, use its service account and a fresh registration token at the new repository URL; preserve labels. **Cutover:** labels and runner IDs retained. `GLORY11` briefly showed offline, then Richard restarted it; both now show online. The dedicated checkout smoke remains to be run. |
| Richard | Webhooks, deploy keys, GitHub Apps, collaborators and package associations retained | Compare Settings pages at both owners and test each app's normal access | No repository webhooks or deploy keys; sole direct collaborator `richardorchard` (admin); eight apps listed below; personal package page shows no packages. **Cutover:** zero webhooks and deploy keys; `richardorchard` remains a collaborator. App access requires representative post-transfer checks. |
| Richard | Azure OIDC and other owner-specific integrations work without broader access | Complete #1736's subject/credential matrix; run read-only Azure checks, OpenTofu plan, next dev deploy, Sentry and bot access checks | **Cutover:** all five immutable OIDC subjects and Azure logins passed in run `35993948845`; read-only OpenTofu backend/state check passed. Old credentials retained. The production setting-name read check passed in run `35994854205`; a normal post-transfer dev deploy remains. |
| Richard | Active clone and runner remotes use new URL after transfer | `git remote -v` and authenticated fetch on Windows, Mac and runner workspaces | **Cutover:** this Mac checkout and shared worktrees now use the organization URL; authenticated `git ls-remote` returned the unchanged `main` SHA. Windows workstation clone remote still needs owner verification; runner checkout smoke is pending. |
| Richard | Scheduled jobs and production promotion still work on their normal paths | Check next scheduled runs; validate dev push deploy. Production tag only in a normal release window | Schedules and release paths listed below. **Cutover:** next scheduled runs and normal post-transfer dev deploy remain to be observed. Production tag promotion was not triggered solely for the transfer. Do not create a production tag solely for this transfer. |

## Environment baseline

Every environment has **zero environment secrets**. `protected` below means only protected branches; the custom patterns are shown exactly. Reviewer gates are named because a personal-to-organization transfer can affect membership. Variables are names only.

| Environment | Policy / reviewer | Variable names | Expected jobs | Result |
| --- | --- | --- | --- | --- |
| `dev-data-refresh` | protected; reviewer `richardorchard` | `ARM_CLIENT_ID`, `ARM_SUBSCRIPTION_ID`, `ARM_TENANT_ID` | `refresh-dev-snapshot.yml` snapshot job | Names, policies, and reviewers verified before and after transfer. |
| `dev-deploy` | protected; no reviewer | `ARM_CLIENT_ID`, `ARM_SUBSCRIPTION_ID`, `ARM_TENANT_ID`, `BITWARDEN_APP_SERVICE_DEPLOY_SECRETS` | `deploy-dev.yml` changes/configure/deploy; `dev-member-auth-e2e.yml` | Names, policies, and reviewers verified before and after transfer. |
| `dev-migrate` | protected; no reviewer | `BITWARDEN_APP_SERVICE_DEPLOY_SECRETS` | `deploy-dev.yml` migrate | Names, policies, and reviewers verified before and after transfer. |
| `opentofu-plan` | protected; no reviewer | `ARM_CLIENT_ID`, `ARM_SUBSCRIPTION_ID`, `ARM_TENANT_ID`, `TF_STATE_CONTAINER`, `TF_STATE_KEY`, `TF_STATE_RESOURCE_GROUP`, `TF_STATE_STORAGE_ACCOUNT` | `opentofu-plan.yml`, `opentofu-apply.yml` plan, `opentofu-drift.yml`, backend smoke plan | Names, policies, and reviewers verified before and after transfer. |
| `opentofu-apply` | protected; reviewer `richardorchard` | Same ARM and `TF_STATE_*` names as `opentofu-plan` | `opentofu-apply.yml` apply/verify, backend smoke apply | Names, policies, and reviewers verified before and after transfer. |
| `prod-data-read` | custom `main`; no reviewer | `BITWARDEN_APP_SERVICE_DEPLOY_SECRETS` | `nightly-legacy-checks.yml` sync, mirror resync | Names, policies, and reviewers verified before and after transfer. |
| `prod-deploy` | custom `main`, `v*`; no reviewer | `ARM_CLIENT_ID`, `ARM_SUBSCRIPTION_ID`, `ARM_TENANT_ID`, `BITWARDEN_APP_SERVICE_DEPLOY_SECRETS` | `deploy.yml` configure, `app-service-setting-names-check.yml` | Names, policies, and reviewers verified before and after transfer. |
| `prod-google-play` | custom `main`; no reviewer | `BITWARDEN_MOBILE_BUILD_SECRETS`, `SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT` | `publish-android-google-play.yml` publish | Names, policies, and reviewers verified before and after transfer. |
| `prod-release` | custom `main`, `v*`; no reviewer | `BITWARDEN_APP_SERVICE_DEPLOY_SECRETS` | `deploy.yml` migrate/deploy | Names, policies, and reviewers verified before and after transfer. |

## Repository integrations

- **Repository secret names (14):** `APP_STORE_CONNECT_ISSUER_ID`, `APP_STORE_CONNECT_KEY_ID`, `APP_STORE_CONNECT_PRIVATE_KEY`, `AZURE_WEBAPP_PUBLISH_PROFILE`, `BITWARDEN_SECRETS_MANAGER_ACCESS_TOKEN`, `CONNECTIONSTRINGS__QUEENZONELEGACY`, `IOS_DISTRIBUTION_CERTIFICATE_BASE64`, `IOS_DISTRIBUTION_CERTIFICATE_PASSWORD`, `IOS_PROVISIONING_PROFILE_BASE64`, `IOS_SHARE_EXTENSION_PROVISIONING_PROFILE_BASE64`, `IOS_WIDGET_EXTENSION_PROVISIONING_PROFILE_BASE64`, `QUEENZONE_LEGACY_MIGRATION_CONNECTION_STRING`, `SENTRY_AUTH_TOKEN`, `SIXLABORS_LICENSE_KEY`.
- **Repository variable names (13):** `BITWARDEN_APP_SERVICE_DEPLOY_SECRETS`, `BITWARDEN_DEV_AUTH_E2E_SECRETS`, `BITWARDEN_DEV_SNAPSHOT_SECRETS`, `BITWARDEN_IOS_RUNNER_SECRETS`, `BITWARDEN_MOBILE_BUILD_SECRETS`, `BITWARDEN_OPENTOFU_APPLY_SECRETS`, `BITWARDEN_OPENTOFU_PLAN_SECRETS`, `DEV_SNAPSHOT_APPROVAL_GATE_CONFIGURED`, `DEV_SNAPSHOT_READY`, `SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `TELEMETRYDECK_APP_ID`.
- **Installed GitHub Apps:** Azure Pipelines, ChatGPT Codex Connector, Claude, Claude Design Import, Cursor, GitHub Learning Lab, Grok (by xAI), Sentry. Check that each needed app is authorized for the organization-owned repository after transfer. Workflow integrations also use Bitwarden, Azure, CodeQL, Dependabot, Sentry, and mobile store credentials; verify their next representative run. No repository webhook or deploy key was listed.
- **Schedules:** nightly legacy checks 03:00 UTC; mobile device smoke 04:00 and 16:00 weekdays; live-site sweep 06:00; app-setting-name check and dev member-auth browser check 07:00; OpenTofu drift 08:00; artifact pruning every two hours. Inspect live runs for overlap rather than assuming the proposed window is free.
- **Owner-sensitive strings:** The three `infra/bootstrap/` entry points now default to `RichardOrchardOrganisation/QueenZone.Modern` with immutable IDs and staged credential names. `docs/architecture/self-hosted-e2e-runner.md` uses the new runner registration URL. Historic issue and PR links may use GitHub's redirect until a separate documentation cleanup. Do not create a new repository at the old path.

## Transfer sequence and stop rule

1. Finish #1734 and the pre-transfer work of #1736. Refresh this checklist and record the current `main` SHA and active jobs immediately before the agreed quiet window.
2. Transfer the existing repository with the same name and public visibility. Keep merge queue disabled. Verify history, settings, environments, apps, runners and clone remotes using the checks above.
3. Complete #1736's exact OIDC claim and read-only Azure login checks, then verify normal PR CI and the next dev deployment. If any required check, runner, permission or identity is unusable, repair it under the new owner before #1738. A transfer back is a separately assessed rollback.
4. Enable and prove the merge queue in #1738 only after normal operation is validated. Never create a new repository at `richardorchard/QueenZone.Modern`, which would remove the old-path redirect.
